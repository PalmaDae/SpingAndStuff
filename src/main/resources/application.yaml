spring:
  application:
    name: SpringAndStuff
  datasource:
    url: jdbc:postgresql://localhost:5432/fisd_db
    username: postgres
    password: 010909
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      idle-timeout: 30000
      connection-timeout: 20000
      pool-name: SpringHikariPool

info:
  app:
    name: ${spring.application.name}
    version: 0.0.1
    description: FISD Spring project

management:
  endpoints:
    web:
      exposure:
        include: "*"
  info:
    env:
      enabled: true